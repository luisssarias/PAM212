import { Text, StyleSheet, View } from 'react-native'

export default function ActivityScreen() {

    return (
      <View>
        <Text> ActivityScreen Proximamente... </Text>
      </View>
    )
  }

const styles = StyleSheet.create({})