import { Text, StyleSheet, View } from 'react-native'

export default function FlatScreen() {

    return (
      <View>
        <Text> FlatListScreen Proximamente... </Text>
      </View>
    )
  }

const styles = StyleSheet.create({})